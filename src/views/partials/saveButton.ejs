<button
  type="button"
  hx-post="/events/<%= eventId %>/save"
  hx-trigger="click"
  hx-target="this"
  hx-swap="outerHTML"
  class="px-3 py-1 bg-slate-200 rounded hover:bg-slate-300 text-sm"
>
  <%= status === "saved" ? "Unsave" : "Save" %>
</button>